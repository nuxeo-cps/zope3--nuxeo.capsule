=============
Nuxeo Capsule
=============

Nuxeo Capsule is a storage framework relying on a data model based on
the JCR (JSR-168). The nuxeo.capsule package contains base components
for the rest of the system.

Other packages provide specific connectors, for instance nuxeo.jcr holds
JCR-specific code.
